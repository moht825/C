지금부터 특수문자를 출력하는 C코드를 짜볼것이다.
특수문자는 "!@#$%%^&*()\" 와 같은 여러 특수문자가 혼합되어 있는 형태를 사용 할것이다.
이러한 코드는 아래와 같이 작성할수 있다.

#include <stdio.h>

int main() 
{
    printf("\"!@#$%%^&*()\"");
}

이와같이 코드를 작성한 이유는 "(큰따옴표)를 작성하기 위해서는 \(역슬래시)가 필요하고
%를 출력하기 위해서는 %%라고 작성해야 하기 때문에 이러한 형태의 코드가 나온다고 생각하면 된다.
여담으로 \를 출력할 때에도 \\라고 역슬래시를 한번 더 붙여줘야 한다.

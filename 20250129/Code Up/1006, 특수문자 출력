C언어 코드를 짤 때 !@#$%^&*() 를 출력하기 위해서는 

#include <stdio.h>

int main() {
    printf("\"!@#$%%^&*()\"");
}

이런식으로 코드를 짜야한다.
왜 그렇냐고 물어본다면, 큰따옴표 를 출력하기 위해서는 앞에 \(역슬래시)를 붙여줘야 하고, %를 출력하고 싶을때는 %를 한번 더 붙여서 
%% 이런식으로 출력을 해야하기 때문이다.
